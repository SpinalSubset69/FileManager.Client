import { FilenameshortenerPipe } from './filenameshortener.pipe';

describe('FilenameshortenerPipe', () => {
  it('create an instance', () => {
    const pipe = new FilenameshortenerPipe();
    expect(pipe).toBeTruthy();
  });
});
