import { FoldernameshortenerPipe } from './foldernameshortener.pipe';

describe('FoldernameshortenerPipe', () => {
  it('create an instance', () => {
    const pipe = new FoldernameshortenerPipe();
    expect(pipe).toBeTruthy();
  });
});
